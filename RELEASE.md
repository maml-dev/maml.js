# Release

1. Bump version in [`package.json`](package.json)
2. Build artifacts
   ```sh
   npm run build
   ```
3. Commit changes
4. Create a new release on GitHub
5. Publish to npm
   ```sh
   npm publish
   ```
